{
  "name": "root",
  "workspaces": [
    "immer-source",
    "immer-simple",
    "immer-mini"
  ],
  "scripts": {
    "start": "lerna exec -- yarn start",
    "source": "lerna exec --scope immer-source -- yarn start",
    "simple": "lerna exec --scope immer-simple -- yarn start",
    "mini": "lerna exec --scope immer-mini -- yarn start"
  },
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
